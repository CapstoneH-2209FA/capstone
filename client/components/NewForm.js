import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const NewForm = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = NewForm.actions

export default NewForm.reducer
